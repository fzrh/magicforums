$('#flash-messages-container').html("<%=j render partial: 'partials/flash_messages' %>")

<% if @comment.persisted? %>
  $('#comments').prepend("<%=j render partial: 'comment', locals: { comment: @comment, post: @comment.post, extra_class: '' } %>")
  $('#comments-form-container').html("<%=j render partial: 'comment_form', locals: { comment: @new_comment, post: @post } %>")
<% end %>
